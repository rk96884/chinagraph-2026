# CHINAGRAPH Yellow Asset Library v2

This version retains the authentic yellow while introducing variable transparency,
broken wax deposits and grain.

Each mark has three pressure variants:

- `soft` — best behind large text
- `medium` — general-purpose default
- `bold` — ticks, arrows and small annotations

All files are tightly cropped transparent WebP assets.
